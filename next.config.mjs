/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com', 'source.unsplash.com'], // Add the domains you need
      },
};

export default nextConfig;
